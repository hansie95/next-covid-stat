import { Box } from "@chakra-ui/layout";
import React, { FC } from "react";

import Header from "../components/Header";

const Home: FC = () => {
  return (
    <Box>
      <Header />
    </Box>
  );
};

export default Home;
